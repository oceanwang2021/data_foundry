需求：
目前指标分组后，同组指标提示词管理的需求如下：1、指标默认不分组，公用一份采集提示词；如果用户选择对指标进行分组，则每组指标用一份提示词（与现状相同）；2、提示词输入支持分段输入和整体输入，目前只建设整体输入的功能，分段输入的功能后期再完善（包括分段输入提示词、将分段输入的内容拼接为完整提示词）；3、整体输入的提示词，需要与指标进行关联并保存到数据库表中，即需要保证indicator_groups与prompt_template的关联关系；4、整体输入的提示词内，会以{}的形式进行占位符的设置，例如维度列有comcode和comname，在提示词模板中会有{comcode}和{comname}作为占位符，需要根据采集参数列的取值信息对comcode和comname进行占位符的替换，例如在试运行中选择了comname为零跑汽车，comcode为3344180，则对于该试运行的采集任务实例，提示词中应该是comname为零跑汽车，comcode为3344180，试运行与采集接口交互时，给采集接口的入参即为填充具体数值之后的提示词。对占位符进行填充后的提示词应该与具体任务实例绑定，存储在数据库中，前端目前不需要展示。给出你对于上述需求的理解，分析现状的区别，给出你的设计方案（包括前端设计、后端设计、重点关注数据存在哪张表的哪个字段里，需要保证前端输入、后端存储、前端反显数据的一致性）

变更范围：
下面我按“需求理解 -> 当前现状 vs 目标差异 -> 设计方案 -> 数据落库设计 -> 前后端一致性”来梳理。


一、我对需求的理解
你这次想把“指标组提示词”这条链路真正做成一套完整、可落库、可实例化的机制，核心有 4 个点：

1. 指标默认不分组，共用一份提示词
也就是：


默认情况下，所有指标属于一个默认组

这一个组只维护一份提示词模板

这份模板对该组内的所有指标生效


如果用户手工分组：


每个指标组各自维护一份提示词模板

组与提示词一一对应


这点和当前产品交互方向基本一致。


2. 提示词输入支持两种模式，但当前只先做“整体输入”
你希望：


UI 上仍保留“分段输入 / 整体 Markdown 输入”的设计方向

但当前阶段只真正建设“整体输入”的保存与生效链路

分段输入先不作为正式能力闭环，只留后续扩展空间


也就是说，这一阶段不要再把分段输入当正式主路径。


3. 整体输入的提示词，需要和指标组建立稳定关联，并落库
核心要求是：


不是“需求有一份提示词”

而是“指标组有一份提示词模板”


所以要保证：


indicator_group_id ↔ prompt_template

多组时每组各有一份

不分组时默认组也有一份


并且要做到：


前端输入能保存

后端能正确存储

前端刷新后能正确反显



4. 模板中的 {placeholder} 要在任务实例级完成替换，并与任务实例绑定
例如模板里有：


{comcode}

{comname}


实际在试运行或正式采集中，如果某条参数行是：


comname = 零跑汽车

comcode = 3344180


那么真正给采集接口的提示词必须已经渲染成：


comname = 零跑汽车

comcode = 3344180


而不是把 {comname} 原样传过去。

而且这个“替换后的最终提示词”不是只临时在内存里算一下，而是要：


和具体采集任务实例绑定

落到数据库

前端当前不展示，但后续可以追溯


这点非常关键。


二、当前现状与目标的差异

1. 当前“指标默认不分组 / 用户可分组”机制
当前已有
前端任务页已经有这套分组机制：


默认组逻辑在 RequirementTasksPanel.tsx (line 122)

默认组构造在 RequirementTasksPanel.tsx (line 125)


也就是：


默认不分组时，会生成一个默认指标组

用户分组后，则使用用户定义组


与目标差异
这一点本身差异不大，现状基本可复用。


2. 当前提示词输入：分段输入和整体输入都在 UI 上存在，但都没有真正成为稳定落库主路径
当前前端状态
在任务页：



分段输入修改的是 indicatorGroups[].promptConfig

在 RequirementTasksPanel.tsx (line 751)




整体 Markdown 草稿放在 promptMarkdownDrafts

在 RequirementTasksPanel.tsx (line 167)




点击保存时会走：


buildWideTableWithPromptDrafts(...) in RequirementTasksPanel.tsx (line 787)


这里会做两件事：

如果当前是 Markdown 模式，就把 Markdown 解析成 promptConfig
再根据 promptConfig 重新生成 promptTemplate

也就是说当前逻辑是：


Markdown 不是直接独立保存

最终仍会被回写成 promptConfig + promptTemplate


差异
而你的目标是：


当前阶段正式只做“整体输入”

分段输入后续再完善


所以现在的差异是：


当前代码把“分段输入”和“Markdown 输入”混在一条保存链里

未来应收敛成：整体 Markdown 才是当前唯一正式编辑入口

分段输入先不作为正式保存主路径



3. 当前提示词和指标组的关联：前端有，后端存储设计上也有，但“任务页保存链路”不完整
设计上已有
前端和数据模型里，指标组上已经有：


promptTemplate

promptConfig


定义在 types.ts (line 186)

后端宽表模型也有：


wide_tables.indicator_groups_json


对应字段在：


WideTableMapper.java (line 15)


这说明设计上已经是“指标组 JSON 挂在 wide_tables 上”。

但当前的真实问题
任务页现在保存提示词走的是：


/api/requirements/{requirementId}/wide-tables/{wideTableId}/preview

前端调用在 api-client.ts (line 1546)

任务页入口在 RequirementTasksPanel.tsx (line 866)


前端虽然把 indicator_groups 一并提交了：


api-client.ts (line 1576)


但后端 persistWideTablePreview(...) 当前只真正处理了：


rows

scope_import

record_count / status


位置在：


RequirementAppService.java (line 243)


它没有把请求里的 indicator_groups 写回 wide_tables.indicator_groups_json。

差异
所以当前差异是：


前端“以为自己保存了提示词”

实际 /preview 后端未落库 indicator_groups_json

导致“关联关系并没有真正稳定持久化”


这是当前最大的缺口之一。


4. 当前任务生成链路没有把 prompt_template 带到任务实例
当前任务规划里做了什么
后端 TaskPlanAppService.parseIndicatorGroups(...) 现在只解析了：


id

name

indicator_columns


位置在：


TaskPlanAppService.java (line 747)


而 TaskPlanDomainService.IndicatorGroup 当前定义也只有：


id

name

indicatorColumns


位置在：


TaskPlanDomainService.java (line 244)


也就是说：


任务生成时只知道“这个任务属于哪个指标组”

但并不知道这个组对应的提示词模板是什么


差异
而你的目标是：


每个任务实例都应该拿到它所属指标组对应的提示词模板

并在生成任务时就完成占位符替换

最终把替换后的完整提示词绑定到任务实例


这条链路当前是完全没接上的。


5. 当前 fetch_tasks 表里没有“渲染后的最终提示词”字段
当前 fetch_tasks 只有：


indicator_group_id

indicator_group_name

indicator_keys_json

dimension_values_json

business_date

row_binding_key

等


定义见：


FetchTask.java (line 7)

FetchTaskRecord.java (line 7)

FetchTaskMapper.java (line 13)


没有任何字段用于保存：


提示词模板原文

或占位符替换后的最终提示词


差异
这和你的目标直接冲突。


三、我的设计方案
我建议分成 两层存储模型：


A. 模板层：指标组级模板
存什么
保存“哪个指标组对应哪份提示词模板”。

存哪

表：wide_tables

字段：indicator_groups_json


存储结构建议
每个指标组对象里至少保留：


id

name

indicator_columns

prompt_template


当前阶段 prompt_config 可以保留兼容，但不再作为主入口。

即：

json



[
  {
    "id": "ig_default_xxx",
    "name": "默认分组",
    "indicator_columns": ["ALGODESC", "COMPUTEPOWERDESC"],
    "prompt_template": "你需要采集 {comname} / {comcode} ..."
  }
]



为什么放这里
因为这是“宽表/需求配置级”信息，不是任务实例级信息。


B. 实例层：任务实例级最终提示词
存什么
保存“某个具体采集任务实例，最终实际发送给采集接口的完整提示词文本”。

这是占位符替换完成后的结果。

存哪
我建议放到：


表：fetch_tasks

新增字段：

prompt_template_text 或 rendered_prompt_text





我更推荐命名为：


rendered_prompt_text


因为它表达的是：


已经根据参数完成渲染后的最终提示词


是否还需要存模板快照
建议再加一个可选字段：


prompt_template_snapshot


含义：


任务创建时使用的原始模板快照


这样后续可区分：

原始模板是什么
渲染后的最终文本是什么

但如果你希望先最小化改动，至少要有 rendered_prompt_text。


四、前端设计方案

1. 指标默认组 / 用户分组保持现有模型
这个可以直接复用当前逻辑：


默认不分组 -> 一个默认组

用户分组 -> 多组


前端无需大改结构。


2. 当前阶段把“整体输入 Markdown”作为正式唯一编辑入口
建议：


保留“分段编辑”按钮/入口可以先隐藏或置灰

当前正式保存只认 promptTemplate

promptConfig 暂不作为主数据来源


这样可以避免出现：


分段改了

Markdown 又反解析

两边互相覆盖

最终数据语义不清


前端页面建议
在提示词编辑区：


当前只保留一个大的 Markdown textarea

每个指标组各自编辑自己的 promptTemplate

每个组单独保存或统一保存都可以



3. 提示词保存统一走“宽表定义更新接口”，不要再复用 /preview
当前提示词保存走 /preview 是不合适的，因为 /preview 的主职责是：


预览行

采集参数行

scope 导入


而不是“正式更新宽表的指标组模板”。

建议改成
前端保存提示词时改走：


宽表更新接口

即走 updateWideTableForRequirement(...) 这条正式链路


因为后端这条链路会真正写：


indicator_groups_json


位置在：


RequirementAppService.java (line 161)


前端收益
这样可以保证：


前端输入

后端落库

前端刷新反显


三者真正一致。


五、后端设计方案

1. 宽表定义层：保证 indicator_groups_json 成为唯一模板事实来源
要做的事

wide_tables.indicator_groups_json 中正式保存 prompt_template

当前阶段 prompt_config 可选保留

任务页前端刷新时，只从这里回读


重点
前端保存提示词时，后端必须真正更新：


wide_tables.indicator_groups_json



2. 任务规划层：把 prompt_template 带入任务生成链路
当前缺口
TaskPlanAppService.parseIndicatorGroups(...) 现在没有解析模板字段。

需要改成
任务规划里的 IndicatorGroup 扩展为：

java



public static class IndicatorGroup {
  public String id;
  public String name;
  public List<String> indicatorColumns = new ArrayList<String>();
  public String promptTemplate;
}



同时 parseIndicatorGroups(...) 要补解析：


prompt_template


这样任务生成时才能知道：


当前任务所属组

当前组对应模板是什么



3. 占位符替换层：在生成 fetch_task 时完成模板渲染
替换来源
占位符值来源应来自任务实例对应的参数值，也就是：


dimension_values_json

或更准确地说，当前任务对应的 ParameterRow.values


例如：

json



{
  "COMNAME": "零跑汽车",
  "COMCODE": "3344180"
}



替换规则
模板中出现：


{comname}

{COMNAME}

{comcode}

{COMCODE}


需要有统一规则。

我建议：


统一大小写不敏感映射

参数键内部保留原始列名

替换时同时支持列名原样和小写形式


例如：


{COMNAME} -> 零跑汽车

{comname} -> 零跑汽车


替换时机
建议在生成 fetch_task 实体时完成，不要拖到执行时再做。

原因：

任务实例一旦生成，应该固化执行上下文
后续就算模板被改了，历史任务也不受影响
便于追溯“这个任务当时到底用的是什么提示词”


4. 实例层落库：fetch_tasks 新增渲染后提示词字段
建议新增字段
表：fetch_tasks

新增至少一个字段：


rendered_prompt_text TEXT / LONGTEXT


可选再加：


prompt_template_snapshot TEXT


任务生成时写入
在 TaskPlanAppService.createTrialRun(...) 和正式计划生成时，构造 FetchTask 时同时写：


indicatorGroupId

indicatorGroupName

renderedPromptText


这样就满足你说的：


替换后的提示词和具体任务实例绑定

保存到数据库

前端当前先不展示



5. 采集接口调用层：直接使用 fetch_task.rendered_prompt_text
后续真正发起采集接口调用时，不再临时拼模板。

而是直接从当前任务实例读取：


rendered_prompt_text


传给采集接口。

这样能保证：


试运行时发送的是已替换文本

正式采集时发送的也是已替换文本

同一个任务实例多次执行，使用的提示词上下文一致



六、重点数据应该存在哪张表哪个字段
我给你明确落库口径。


1. 指标组与模板的关联
存储位置

表：wide_tables

字段：indicator_groups_json


存储内容
每个组对象保存：


id

name

indicator_columns

prompt_template


当前阶段这是主数据源。


2. 采集参数行
存储位置

表：wide_table_rows

字段：dimension_values_json


存储内容
每一行采集参数的列值，例如：

json



{
  "COMNAME": "零跑汽车",
  "COMCODE": "3344180"
}



这已经是你们当前采集参数事实来源，继续沿用。


3. 任务实例绑定的最终提示词
存储位置
建议新增：


表：fetch_tasks

字段：rendered_prompt_text


可选：


prompt_template_snapshot


存储内容
例如：

rendered_prompt_text：

text



请采集零跑汽车（3344180）的算力描述和算法描述...




七、前端输入、后端存储、前端反显的一致性方案

A. 前端输入
来源：


任务页“采集提示词管理”

当前阶段只编辑 promptTemplate


保存对象：


wideTable.indicatorGroups[].promptTemplate



B. 后端存储
正式存储：


wide_tables.indicator_groups_json


不再依赖 /preview 顺带保存。


C. 前端反显
页面刷新后：


重新读取 wide_tables.indicator_groups_json

反序列化为 indicatorGroups

直接回填 promptTemplate


这样可以保证：


前端输入什么

后端存什么

前端刷新回来看到什么


完全一致。


D. 任务实例生成
任务创建时：

根据 indicator_group_id 找到对应 prompt_template
根据采集参数行 dimension_values_json 替换占位符
把替换后的结果写到 fetch_tasks.rendered_prompt_text


E. 任务执行
调用采集接口时：


不再现场拼装模板

直接用 fetch_tasks.rendered_prompt_text


这样保证执行一致性。


八、我建议的实施顺序
第一阶段：先把模板落库和反显打通
前端只保留整体 Markdown 正式保存
提示词保存改走宽表正式更新接口
后端确保写入 wide_tables.indicator_groups_json
前端刷新正确反显

第二阶段：打通任务实例级模板渲染
扩展任务规划 IndicatorGroup，带上 promptTemplate
生成试运行/正式任务时，按参数行替换占位符
fetch_tasks 新增 rendered_prompt_text
采集执行接口改用该字段

第三阶段：后续再完善分段输入
分段输入正式可编辑
分段输入自动拼接成完整模板
和 Markdown 双向同步或确定唯一主数据源


九、一句话总结
你的目标本质上是建立两层数据：


模板层


指标组 ↔ 提示词模板

存在 wide_tables.indicator_groups_json




实例层


采集任务实例 ↔ 替换后的最终提示词

建议存在 fetch_tasks.rendered_prompt_text




当前最大的问题有两个：


提示词保存链路没有真正稳定落到 wide_tables.indicator_groups_json

任务生成链路没有把 prompt_template 带入，也没有任务实例级最终提示词存储位


变更计划：
按“先打通模板保存，再打通任务实例渲染，最后收口兼容逻辑”的顺序推进。

第一阶段：提示词模板保存与反显收敛


前端收敛编辑入口

目标：当前阶段只把“整体 Markdown 输入”作为正式可用能力。

变更：


在 RequirementTasksPanel.tsx 中保留整体输入区作为主编辑区。

分段输入区先降级为占位/只读提示，避免继续参与正式保存。

前端状态以 indicatorGroups[].promptTemplate 为主，不再把 promptConfig 作为当前阶段主事实来源。




前端保存接口改造

目标：提示词保存不再走 /preview，而是走正式宽表更新链路。

变更：


在 api-client.ts 增加或复用“更新宽表定义”的调用方法，确保会提交 indicator_groups。

在 RequirementTasksPanel.tsx 中把“保存提示词”从 persistWideTablePreview(...) 切到正式宽表更新接口。




后端宽表保存链路确认与补齐

目标：把指标组提示词真正写入 wide_tables.indicator_groups_json。

变更：


复用 RequirementAppService.java (line 150) 这条 updateWideTableForRequirement(...) 链路。

确认 WideTableMapper.java 已能更新 indicator_groups_json。

如有必要，补控制器或前端调用适配，但不再依赖 /preview 顺带写提示词。




前端反显统一

目标：刷新页面后，从数据库稳定反显指标组模板。

变更：


在 api-client.ts 保持 raw.prompt_template -> group.promptTemplate 的映射为唯一主反显来源。

若 prompt_config 仍存在，仅作为兼容字段保留，不主导当前页面展示。





第二阶段：默认组与用户分组的模板关联固化


默认组模板规则固化

目标：未分组时所有指标共用一份模板。

变更：


在 RequirementTasksPanel.tsx 继续沿用默认组生成逻辑。

保存时保证默认组也写入 indicator_groups_json，而不是只在前端临时存在。




用户分组模板规则固化

目标：用户分组后，每组一份模板。

变更：


保存时每个组对象都包含：

id

name

indicator_columns

prompt_template




前端编辑、保存、刷新后反显都围绕这一结构工作。





第三阶段：任务生成链路接入模板


后端任务规划模型扩展

目标：任务规划时能拿到组对应的模板。

变更：


修改 TaskPlanDomainService.java 中的 IndicatorGroup 结构，新增：

promptTemplate




修改 TaskPlanAppService.java 中 parseIndicatorGroups(...)，解析 indicator_groups_json 里的 prompt_template。




试运行与正式任务生成统一接入模板

目标：不管试运行还是正式生成任务组，都能按组拿到模板。

变更：


在 TaskPlanAppService.java 的：

createTrialRun(...)

正式任务组生成链路

中，把组模板传入任务草稿/任务实例构建流程。








第四阶段：占位符替换与任务实例绑定


占位符渲染器实现

目标：把 {comcode}、{comname} 替换成参数行实际值。

变更：


在后端新增一个小型模板渲染方法，建议放在 TaskPlanAppService.java 或抽成独立 helper。

输入：

promptTemplate

当前任务对应的参数值 Map<String, String>




输出：

renderedPromptText




规则：

支持 {COMCODE} / {comcode} 这类大小写兼容替换

只替换当前参数行已有的键

未命中的占位符可保留原样或记日志，具体实现时再定







参数值来源统一

目标：占位符替换以采集参数行事实来源为准。

变更：


继续以 wide_table_rows.dimension_values_json 反推出的参数行为准。

任务生成时使用当前任务对应的参数行值，不再从 dimensionRanges 做笛卡尔推导。





第五阶段：数据库增加任务实例级最终提示词存储

新增任务实例字段

目标：把“替换后的最终提示词”绑定到任务实例。

建议新增字段：

表：fetch_tasks

字段：rendered_prompt_text

类型：TEXT 或 LONGTEXT




可选新增：


prompt_template_snapshot

用于保留任务创建时的原始模板快照


后端模型与 Mapper 扩展

变更文件：

FetchTask.java

FetchTaskRecord.java

FetchTaskMapper.java




需要补：


查询字段

插入字段

领域模型字段映射


数据库脚本

变更：


新增 migration / SQL 脚本，为 fetch_tasks 加字段：

rendered_prompt_text

可选 prompt_template_snapshot






第六阶段：任务生成时写入最终提示词

试运行任务写入最终提示词

目标：试运行实例创建时就固化最终提示词。

变更：

在 TaskPlanAppService.java 的 createTrialRun(...) 中：

找到当前任务所属 indicatorGroup

取其 promptTemplate

用当前参数行值替换占位符

写入 fetchTask.renderedPromptText






正式任务实例写入最终提示词

目标：正式任务组生成时同样固化最终提示词。

变更：

在正式任务组/采集任务生成链路中，同步增加上述逻辑。

保证试运行和正式采集用同一套模板渲染规则。





第七阶段：采集执行链路改用实例级最终提示词

调用采集接口时使用 rendered_prompt_text

目标：交互给采集接口的是已填值后的最终提示词。

变更：

找到实际执行采集任务的后端服务

将原本可能临时拼装提示词的逻辑，改成直接读取：

fetch_tasks.rendered_prompt_text




确保执行时提示词不再依赖运行时重新拼装





第八阶段：兼容与收口

分段输入暂时降级处理

当前阶段策略建议：

前端可保留入口，但不作为正式保存主路径

或直接隐藏，仅保留整体 Markdown 输入

promptConfig 继续保留在结构里，避免影响旧数据读取



历史数据兼容

目标：避免已有需求/宽表因旧结构无法展示。

变更：

前端反显时：

优先 promptTemplate

若旧数据只有 promptConfig，可按旧逻辑临时拼一份模板用于展示




后端解析时：

若某组没有 prompt_template，允许回退为空字符串或默认模板








重点涉及文件

前端：


RequirementTasksPanel.tsx

api-client.ts

types.ts

可能涉及 indicator-group-prompt.ts


后端：


RequirementAppService.java

WideTableMapper.java

TaskPlanAppService.java

TaskPlanDomainService.java

FetchTask.java

FetchTaskRecord.java

FetchTaskMapper.java


数据库：


wide_tables.indicator_groups_json

fetch_tasks.rendered_prompt_text

可选 fetch_tasks.prompt_template_snapshot



本次执行顺序建议

先改提示词保存口径到 wide_tables.indicator_groups_json
再改前端反显，确认模板与指标组关联稳定
再扩任务规划模型，带上 prompt_template
再加 fetch_tasks 新字段
再做占位符替换和任务实例写入
最后改采集执行链路，读取 rendered_prompt_text